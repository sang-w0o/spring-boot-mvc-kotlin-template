package com.template.unit.user

import com.template.unit.BaseUnitTest

/**
 * UserService#getInfo()에 대한 단위 테스트를 작성합니다.
 * 이 메소드는 `JwtTokenUtil#getAuthentication()`에 매우 의존적이므로
 * `JwtTokenUtil#authentication()`에 대한 단위 테스트를 작성하는 것으로 대체했습니다.
 */
class UserGetInfoTest : BaseUnitTest()
